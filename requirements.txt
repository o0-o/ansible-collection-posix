# Requirements for o0_o.posix collection

# Core dependencies
jc>=1.25.0                  # Command output parsing

# Dependencies for o0_o.utils.hostname filter (used by uname filter)
dnspython>=2.0.0            # DNS resolution
idna>=3.0                   # IDN support
tldextract>=3.1.0           # TLD extraction
