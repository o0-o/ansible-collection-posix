# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible
# -*- mode: yaml; yaml-indent-offset: 2; indent-tabs-mode: nil; -*-
---
bugfixes:
  - >-
    facts - unknown subset names in gather_subset fail in negated
    form exactly as they do in positive form. !bogus was a silent
    no-op, so a typo'd exclusion gathered exactly what it meant to
    exclude.
