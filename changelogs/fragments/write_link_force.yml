# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible
# -*- mode: yaml; yaml-indent-offset: 2; indent-tabs-mode: nil; -*-
---
bugfixes:
  - >-
    write - C(force) now gates repointing an existing link under
    C(state=link). With C(force=false) the task fails rather than
    silently leaving the path aimed at a target the play did not name;
    previously the link repointed unconditionally.
