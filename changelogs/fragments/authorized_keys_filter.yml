# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible
# -*- mode: yaml; yaml-indent-offset: 2; indent-tabs-mode: nil; -*-
---
minor_changes:
  - 'authorized_keys filter - Added filter to parse SSH ``authorized_keys`` file content into structured data with support for key types, options, and comments.'
  - 'authorized_keys_utils - Added comprehensive parser for SSH ``authorized_keys`` format including support for key restrictions and options.'
