# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible
# -*- mode: yaml; yaml-indent-offset: 2; indent-tabs-mode: nil; -*-
---
minor_changes:
  - 'who - New action/module pair that gathers session information using the ``who`` command with timezone-aware parsing.'
  - 'who - Action plugin detects target system timezone using ``date +%z`` to correctly parse login timestamps and calculate epoch values.'
