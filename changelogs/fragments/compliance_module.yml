# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible
# -*- mode: yaml; yaml-indent-offset: 2; indent-tabs-mode: nil; -*-
---
major_changes:
  - 'Added ``compliance`` module to detect POSIX and UNIX standard support on managed hosts using ``getconf`` fallbacks.'
