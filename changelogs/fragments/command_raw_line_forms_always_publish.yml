# vim: ts=2:sw=2:sts=2:et:ft=yaml.ansible
# -*- mode: yaml; yaml-indent-offset: 2; indent-tabs-mode: nil; -*-
---
bugfixes:
  - >-
    command - raw executions with empty output, check-mode predictions
    included, now publish ``stdout_lines`` and ``stderr_lines`` as empty
    lists instead of omitting them. ansible-core 2.21 injects the keys
    into action results itself, which masked the omission; 2.19 and 2.20
    do not, and every check-mode row of the distro matrix failed there.
